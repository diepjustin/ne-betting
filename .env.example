# No credentials are needed for Phase 1: every endpoint used is public.
# Only the contact string sent in the User-Agent header lives here.
HUSKER_MARKETS_CONTACT="Justin Diep, The Daily Nebraskan, you@example.com"
